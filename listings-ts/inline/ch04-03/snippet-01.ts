// Rust: fn first_word(s: &String) -> ?
// 반환 타입을 아직 모르는 상태 - 문자열의 일부를 어떻게 반환할까?
// JS에서는 string을 반환하면 되지만, Rust에서는 소유권 문제가 있습니다
function firstWord(s: string): unknown {
    // 아직 반환 타입을 결정하지 못했습니다...
}
