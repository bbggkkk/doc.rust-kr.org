function main() {
    // 덧셈
    const sum = 5 + 10;

    // 뺄셈
    const difference = 95.5 - 4.3;

    // 곱셈
    const product = 4 * 30;

    // 나눗셈
    const quotient = 56.7 / 32.2;
    const truncated = Math.trunc(-5 / 3); // Rust 정수 나눗셈은 버림 → Math.trunc

    // 나머지 연산
    const remainder = 43 % 5;
}

main();
