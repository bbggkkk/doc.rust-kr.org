// JavaScript로 보기 — unsafe는 JS에 없는 개념입니다
// Rust: unsafe 함수 정의 및 unsafe 블록 내에서 호출
// JavaScript에는 unsafe 개념이 없습니다.
// 모든 함수는 자유롭게 호출할 수 있습니다.

function dangerous() {
  // JavaScript에서는 어떤 함수든 호출할 수 있습니다
}

dangerous(); // unsafe 블록 없이 바로 호출 가능
