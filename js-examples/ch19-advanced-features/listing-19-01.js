// 예제 19-1: 원시 포인터 생성하기
// unsafe 및 원시 포인터는 JS에 없는 개념입니다.
// JS는 가비지 컬렉터가 메모리를 자동으로 관리하므로
// 포인터를 직접 다룰 필요가 없습니다.

let num = 5;

// JS에서는 변수가 값을 참조하지만,
// 메모리 주소를 직접 다루는 것은 불가능합니다.
let ref1 = num; // 값 복사 (원시 타입)
let ref2 = num; // 값 복사

console.log(`ref1: ${ref1}, ref2: ${ref2}`);
