// 문자열 슬라이싱

const hello = "Здравствуйте";

// JavaScript에서는 문자 단위로 슬라이싱합니다
const s = hello.slice(0, 2); // "Зд" — 처음 2개 문자

// Rust에서는 바이트 범위로 슬라이싱합니다
// &hello[0..4]는 첫 4바이트 = "Зд" (각 문자가 2바이트)
// &hello[0..1]처럼 문자 바이트의 일부만 슬라이싱하면 패닉이 발생합니다
