// 문자열 인덱스로 바이트에 접근하기
// JavaScript에서는 문자 단위로 접근할 수 있지만 바이트 단위는 아닙니다

const hello = "Здравствуйте";
const answer = hello[0]; // "З" — JavaScript는 전체 문자를 반환합니다

// Rust에서는 &hello[0]이 허용되지 않습니다
// UTF-8에서 "З"은 2바이트(208, 151)이므로
// 인덱스 0의 바이트만으로는 유효한 문자가 아니기 때문입니다
