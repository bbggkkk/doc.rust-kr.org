// Rust의 fn main()은 JS에서 별도의 진입점 함수가 필요 없습니다.
// JS 파일은 위에서 아래로 자동 실행됩니다.

// 브라우저 환경이라면:
// <script> 태그 안의 코드가 바로 실행됩니다.

// Node.js 환경이라면:
// node main.js 로 파일을 실행하면 코드가 바로 실행됩니다.

function main() {
  // 함수 본문
}

main(); // 직접 호출해야 합니다
