// Rust에서는 let으로 선언한 변수에 재할당하면 컴파일 에러가 발생합니다.
// JS에서는 let으로 선언해도 재할당이 가능하므로, 이 코드가 정상 실행됩니다.
let x = 5;
console.log(x); // 5
x = 6; // JS에서는 에러 없이 동작합니다
console.log(x); // 6
// Rust는 이 에러를 컴파일 타임에 잡아냅니다.
// JS에서 불변을 원한다면 const를 사용하세요.
