// Rust에서는 함수 본문의 마지막에 세미콜론을 붙이면 구문이 되어
// 값을 반환하지 않으므로 컴파일 에러가 발생합니다.
// JS에서는 return 키워드로 명시적으로 반환해야 합니다.
function plusOne(x) {
  return x + 1; // return이 없으면 undefined를 반환합니다
}
// Rust는 세미콜론 하나로 반환 여부가 달라지는 것을 컴파일 타임에 검사합니다.
