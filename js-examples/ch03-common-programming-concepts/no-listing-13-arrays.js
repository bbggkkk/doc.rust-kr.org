// Rust 배열: 고정 길이, 같은 타입만, 스택에 할당
// JS 배열: 가변 길이, 아무 타입, 힙에 할당
const a = [1, 2, 3, 4, 5];

// Rust에서 배열 길이 변경 불가 — JS에서는 자유롭게 가능
a.push(6); // JS: OK ✅ / Rust: 컴파일 에러 ❌
// Rust에서 가변 길이가 필요하면 Vec<T> 사용 (ch08에서 다룸)
