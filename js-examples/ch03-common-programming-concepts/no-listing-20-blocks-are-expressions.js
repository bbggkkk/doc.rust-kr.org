// Rust: 블록 {}이 표현식으로서 값을 반환할 수 있음
// JS: 블록은 값을 반환하지 않음 — IIFE나 변수로 대체
const y = (() => {
    const x = 3;
    return x + 1; // Rust에서는 세미콜론 없이 x + 1 이 반환값
})();
// y = 4

// 핵심 차이: Rust에서 { let x = 3; x + 1 } 자체가 4를 반환하는 표현식
// JS에서는 블록이 값을 생성하지 않으므로 함수로 감싸야 함
