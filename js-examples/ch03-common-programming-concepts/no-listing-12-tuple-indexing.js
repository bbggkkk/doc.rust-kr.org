// Rust: let x = (500, 6.4, 1);
//       let five_hundred = x.0;
// JS에서는 배열의 인덱스로 접근합니다.
const x = [500, 6.4, 1];
const fiveHundred = x[0]; // 500
const sixPointFour = x[1]; // 6.4
const one = x[2]; // 1
