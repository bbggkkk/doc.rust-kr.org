// Rust의 Some(값) → JS에서는 그냥 값 자체
// Rust의 None → JS에서는 null 또는 undefined
const someNumber = 5;       // Some(5)
const someChar = 'e';       // Some('e')
const absentNumber = null;  // None — JS에서는 타입 명시가 불가능
