// Rust의 trait → JS에서는 인터페이스 개념이 없지만,
// 클래스가 특정 메서드를 가져야 한다는 "약속"으로 표현할 수 있습니다.
// TypeScript라면 interface로 정의합니다:
// interface Summary { summarize(): string; }

// JS에서는 덕 타이핑으로 summarize() 메서드만 있으면 됩니다
