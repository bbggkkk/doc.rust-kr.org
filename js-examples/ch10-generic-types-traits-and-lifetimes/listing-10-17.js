// Rust의 대여 검사기가 라이프타임 'a와 'b를 비교하여
// 'b가 더 짧은 것을 감지하고 컴파일 에러를 발생시킵니다.
// JS에서는 GC가 메모리를 관리하므로 이런 분석이 불필요합니다.
let r;
{
    const x = 5;
    r = x; // JS: 값 복사, Rust: 참조 대여
}
console.log(`r: ${r}`); // JS에서는 문제없이 5가 출력됩니다
