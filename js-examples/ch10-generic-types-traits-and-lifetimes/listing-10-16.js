// Rust에서는 스코프를 벗어난 값의 참조가 댕글링 참조가 되어 컴파일 에러가 발생합니다.
// JS는 가비지 컬렉터(GC)가 메모리를 관리하므로 댕글링 참조가 발생하지 않습니다.
let r;
{
    const x = 5;
    r = x; // JS에서는 값이 복사되므로 문제없습니다
}
console.log(`r: ${r}`); // 정상 동작 — JS에는 라이프타임 개념이 없습니다
